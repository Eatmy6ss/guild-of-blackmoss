# 黑苔公会：开发接续入口

- 新任务或上下文恢复时，先读 `docs/development/HANDOFF.md`；开始具体工作前核对当前分支、提交和未提交改动。文件中的版本是记录，不保证仍是最新。
- 按任务需要读取 `docs/development/BACKLOG.md` 的对应条目和 `docs/development/DECISIONS.md` 的相关决策。想快速了解"游戏现在有什么/缺什么/什么挂起"，读 `docs/development/SYSTEM-MAP.md`（全景盘点，制作人侧维护）。无需每次重读全部设计历史或整份审查报告。
- 用户当前明确指示优先。区分“当前实现事实”“用户已确认需求”“审查建议/待决事项”。不要把建议或历史草案视为已批准的改版。
- 当前技术主线是 TypeScript + React + PixiJS + Vite。玩法审查证据在 `docs/gameplay-audit-2026-09-24.md`；历史设计在 `docs/DESIGN.md`，其中有已替代或暂缓的方案。
- 行为修改围绕可验证的小范围目标推进，检查 UI → 状态 → 模拟 → 奖励/存档的完整调用链；按改动运行有意义的验证。不要仅凭类型检查或历史“门禁通过”判断玩法正确。
- 用户明确要求：每轮开发都维护一次 HANDOFF。开发开始写入目标与验收，开发结束写入实际改动、验证、遗留事项与下一步；长任务在阶段完成、方向变化、阻塞或交接时同步更新。普通问答无需制造记录。
- 保持一份权威的当前 HANDOFF。细节放在任务条目或专题文档，通过链接引用；历史沿用 Git，不复制多份互相竞争的当前状态。
- 尊重朋友和其他任务的未提交修改。并行修改同一区域前先划分范围；需要并行开发时使用独立分支/工作树。是否新建任务由用户决定。
- 交接写明：修改文件、已完成/未完成内容、验证结果、尚未验证风险、下一步和提交/PR状态。记录证据，不写令牌、凭据或依赖当前会话的秘密。

## 制作人红线（2026-09-24 制作人侧登记；任何 AI/协作者必须遵守；与旧文档冲突时以本节+DECISIONS.md 最新决策为准）

1. **提交纪律**：改动一律先跑 `npm run verify`（37 门禁；看 EXIT 码与尾部 ✓ 计数，只 grep 计数不算数）。禁止 `--no-verify`（纯 docs 提交除外）。验证工具已入库，干净检出 + `npm install` 即可用。
2. **存档纪律**：改存档结构必须走 `save.ts` 的 MIGRATIONS 逐级迁移链（禁跳级默认分支）；smoke 合成存档同步补字段；**严禁向用户真实 localStorage 写测试数据**（测试用独立 profile/端口）。
3. **结算反馈**：任何带效果的系统（事件/建筑/委托/装备）**不允许静默结算**——影响必须在同一界面可见（先例：事件结果面板的影响明细 chips）；数值类断言改动必须与数值改动同轮提交。
4. **范围红线**：BACKLOG 的 P08 挂起提案池（套装/声望/10 人本/公会保卫战/人物特性/亡灵叙事/版图三/经济方案池/UI 换皮第三批）**未经制作人 grilling 不得设计或实现**；美术/UI 换皮动工前必须先问制作人。
5. **方向基线**（详见 DECISIONS.md U08–U11）：P01 刷本成长循环为核心、公会养成为辅；装备纪元按 U09 融合方案（版图二 T3 机制词缀+T2 横向不作废）；高塔按 U10 大秘境化（装等门槛/奖励做厚/塔纪录只认手动）；死亡激励按 U11 纪念品质+总上限。引擎不换（TS+React+PixiJS+Vite）；竞品《公会创世纪》仅 ~10% 轻度参考。
6. **涌现叙事是宪法级精髓**：随机名号、编年史联动、巫师 3 式两难事件+延迟后果——重构和修复不得抹平这些特性。
