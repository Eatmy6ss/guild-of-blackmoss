# 当前交接

更新时间：2026-09-24（用户授权推送并希望使用中文说明）。本轮目标：将未共享的本地功能提交改为中文说明，推送到origin/feature/shldo-dev，核对远端提交与本地一致，更新交接；不改玩法，沿用已通过且代码未变化的构建/试玩证据。恢复时先核对实际Git状态与用户最新指示；本文件是一份当前入口，不复制多份竞争版本。

## 当前目标与结果

用户与朋友共同开发 `Eatmy6ss/guild-of-blackmoss`。已完成玩法首轮审查和长期接续准备；本轮明确要求每次开发维护HANDOFF，并完整开发王国任务、数值、奖励、背景联系及打磨。

**K01已完成本地开发与定向验证。** 大厅“王国委托”或Q打开，10份一次性来函、最多同时2份，连接建设、Boss和副本。酬金/补给二选一，3件固定装备，150信任上限；20/50/100信任解锁3%/6%/10%药水优惠。来函、结案回信和大事记连通。实际数值、任务表、存档边界及测试证据见 [K01专题](kingdom-commissions.md)。

试玩发现内置浏览器不支持原导入/导出 `prompt()`，本轮补成游戏内面板：备份代码、复制反馈、导入校验/预览、明确替换按钮。无效存档不覆盖当前进度。

## 代码与工作区

- 本机目录：`D:\AIWorkspace\Projects\黑台工会`。
- 远端：`https://github.com/Eatmy6ss/guild-of-blackmoss.git`。
- 本轮分支：`feature/shldo-dev`；开发基准提交：`761de24ab74fc2eaf1b03115585b446e32d66691`。
- 用户反馈“目前没有问题，可以提交”。源码、测试与交接文件纳入本文件所在的本地提交 `功能：新增王国委托与公会成长奖励`；具体提交号以Git日志为准。**尚未推送、创建PR或发布。** 朋友电脑与线上试玩尚未同步本轮功能。
- `shldo`此前已接受协作者邀请并确认可读写；以后需要远端操作时再检查实际权限。Git身份已在仓库本地设置。
- 环境：Node24.15/npm11，依赖已安装。本轮将现有esbuild0.21.5声明为直接开发依赖，锁文件已更新，测试无需临时下载执行器。

核心修改：

| 文件 | 用途 |
|---|---|
| `src/data/kingdom.ts` | 来函、前置、目标、固定奖励、关系阶梯 |
| `src/sim/kingdom.ts` | 接取、撤销、胜利/通关/建设推进、领取、折扣、状态消毒 |
| `src/ui/KingdomPanel.tsx` | 委托板、进度、二选一报酬、后续条件、回信、键盘焦点 |
| `src/ui/SaveTransferPanel.tsx` | 游戏内存档导入导出与预览 |
| `src/App.tsx` | 大厅入口、远征结算、建筑、奖励资产、采购价格、重开清委托、存档接线 |
| `src/state/save.ts` | v12；v11迁移补空王国状态，旧档保留原资产 |
| `src/index.css` | 官契视觉、双栏/窄屏布局、存档面板 |
| `scripts/kingdom.test.ts` / `scripts/test-kingdom.cjs` | 可重复的10项定向测试，内存打包执行 |
| `package.json` / `package-lock.json` | `test:kingdom`入口与直接构建工具依赖 |
| `scripts/dev-save.ts` / `scripts/smoke.ts` | 现有存档夹具补齐新增kingdom字段；没有恢复完整历史门禁 |
| `README.md` | 新玩法/测试入口/v12说明，说明历史verify缺失 |

此前新增的 `AGENTS.md`、本目录文档和 `docs/gameplay-audit-2026-09-24.md` 已一并纳入本次提交范围。没有覆盖朋友的既有未提交源代码改动。

## 验证结果

- `npm.cmd run test:kingdom`：10/10通过。覆盖前置与上限、错误目标、真实run状态机边界、通关/建设、重复领取与二选一、全链数值、迁移/读档/导入导出、异常记录恢复。
- `npm.cmd run build`：TypeScript和Vite生产构建通过。保留既有大包提示，主包约716KB。
- UI实际试玩：建设后补给到账；刷新保留receipt；正常战斗3次推进1/3→3/3；撤退回城交付；20信任后购买治疗药实扣44金。
- UI后期夹具：最后委托双击仅领取300金、1爆发药、1驭火者坠；刷新导出确认已结案10份、物品唯一；全结案提示与10%折扣正确。
- UI旧v11码：保留资产/建筑，王国状态为空；已有训练场接取即1/1。无效码提示明确；导出复制成功。
- 桌面和390px窄屏已检查；委托面板无横向溢出，焦点留在面板、Esc正常关闭，关闭恢复原页面滚动。
- 用户来源是 `http://127.0.0.1:5173/`；本轮测试使用独立 `http://127.0.0.1:5174/`，没有以测试导入覆盖用户来源的存档。旧审查用过的localhost来源也不等于用户当前来源。

## 重要边界与未完成事项

- 沿用原游戏“远征结束才写入资产存档”；途中刷新会回到出征前，委托规则已提示。不要只保存战绩却不保存对应资产。
- 10份委托整链通过纯逻辑测试；只实际试玩了初始建设/3胜，以及经导入的后期领取状态。未手动通关全部副本，未做全职业胜率和长期经济评估。
- 重开函数已清空kingdom，但旧 `confirm()` 路径在内置浏览器兼容性和原重开不清全部建筑/日期/事件是独立历史问题，列F13；未宣称重开全流程已验收。
- 历史 `npm run verify` 依赖的 `scripts/verify.mjs` 缺失；`.gitignore`还忽略 `scripts/*.mjs`。不宣称37项门禁通过，D01仍待恢复；新增测试入口用 `.cjs` 正常可纳入Git。
- F01—F12首轮审查问题仍在 [BACKLOG](BACKLOG.md)，本轮没有顺带全局修复。重点包括跨版图门槛、选路错位、直达Boss、高塔血量、建筑加成、招募专精和事件药水。
- “公会养成＋关键时刻指挥”“5—10分钟远征”等仍是建议，用户未确认，不应作为后续改版既定需求。

## 下一步与运行

本轮K01没有等待用户决策的实现阻塞。可在现有5173页面回到大厅按Q试玩；若服务器已退出，在本目录执行 `npm.cmd run dev -- --host 127.0.0.1 --port 5173 --strictPort`。接续先检查端口，不能依赖聊天里的历史进程号。

后续按用户要求继续新功能或修复条目；需要与朋友共享本轮成果时，再推送本开发分支并按需要创建PR。不要把本地提交描述为远端已发布。

## 每轮接续约定

- 开始时核对Git与当前目标，写本轮范围/验收；阶段完成或方向变化时更新；结束记录实际改动、验证、限制、下一步、提交/PR状态。
- 同一目标可继续当前Codex任务，压缩后从本文件与代码恢复。独立功能可开新任务，不需要每次新开应用窗口。
- 不把聊天摘要当代码事实，不把审查建议当用户批准。详细内容通过专题链接读取。
- 跨工作树/机器之前，确保所需文件已随提交提供或明确复制；未提交的本地文件不会自动出现在普通新工作树中。
