# 开发任务清单

基准：2026-09-24 / `761de24`。审查条目来自[首轮审查](../gameplay-audit-2026-09-24.md)；K01来自用户后续明确需求。以各行状态为准，不代表用户已批准全部开发范围。
**2026-09-24 晚更新（制作人侧）**：K01 已合并主线（de6d2a1）并部署线上试玩；D01 验证工具链已入库（7d441de）；F07 主线已部分修复（41b2a8b，见行内注记）。F01–F13 即当前修复主线的正式清单，按下方建议顺序推进即可。

状态约定：待处理 → 进行中 → 已验证 → 已交付；无法推进时写清阻塞。只有相应验收通过才标已验证；交付补提交或PR引用。每次只展开当前切片，无需一开始为所有任务写实施方案。

## 发布管线（协作须知，2026-09-24 起工具已入库）

- 提交前运行 `npm run verify`（37 门禁：esbuild+smoke+生产构建；看 EXIT 码与尾部 ✓ 计数）。首次克隆后需 `npm install`；`scripts/smoke.mjs` 由 verify 自动重建，属生成物不入库。
- pre-commit 钩子不入 git：本地安装 `git config core.hooksPath hooks`（若仓库补 hooks/ 目录）或从 README 手动接入；没装钩子时请自觉跑 verify，--no-verify 仅限纯 docs 提交。
- **线上试玩（gh-pages）不在合并后自动更新**：部署 = `npx vite build --config vite.config.playtest.ts` → `node scripts/inline-assets.mjs`（56 张素材内联为单文件 dist-playtest/index.html）→ 将该文件作为 gh-pages 分支的 index.html 推送。当前由制作人侧执行；协作者如需接管请先在此行声明。

## 准备与规则一致性

| ID | 任务 | 证据/主要区域 | 最小验收 | 状态 |
|---|---|---|---|---|
| D00 | 长期开发接续文件 | AGENTS、HANDOFF、BACKLOG、DECISIONS | 文件存在、链接可用、事实与建议分开；随K01提交 | 已交付并推送（0ce078e） |
| K01 | 王国委托与关系奖励 | data/kingdom、sim/kingdom、KingdomPanel、App、save、SaveTransferPanel | 10份来函、奖励二选一、防重复领取、折扣采购、旧档迁移；10项定向测试与UI试玩通过，详见[专题](kingdom-commissions.md) | 已合并主线（de6d2a1，PR#1）+已部署线上（gh-pages 8612f60）；制作人试玩中，反馈走此清单 |
| D01 | 恢复可共享的验证入口 | package.json、scripts、.gitignore | 干净检出可安装并运行声明的检查；真实失败可被捕获；不造“空通过”脚本 | **已交付（7d441de）**：verify.mjs/E2E 三脚本/qa-playthrough/diag 三件套/inline-assets 已入库，gitignore 加白名单；残余=钩子本地安装（见上"发布管线"） |
| F01 | 跨版图解锁 | data/regions、App作战板 | 无首版团本首杀时第二版图锁定；达成后开放；锁定提示准确 | 待处理 |
| F02 | 下一段选路错位 | sim/run、App休整 | 选定节点准确控制下一步；战斗/事件/休整/宝箱均保持正确索引和压轴Boss | 待处理 |
| F03 | 直达Boss仍打首场 | App直捣Boss | 达到熟练度后进入目标Boss；无重复旧战；不足阈值不能使用 | 待处理 |
| F04 | 高塔血量连续性 | sim/tower、App塔结算 | 255最大血、战末10血、20%休整后应61；死亡不恢复；进下一层保持正确值 | 待处理 |
| F05 | 建筑效果未应用 | data/base、morale、tower、App | 疗养所治疗和祠堂宴会加成都能观察到；0级与升级值均核对 | 待处理 |
| F06 | 招募候选与入职不一致 | gen、tavern、App hire | 预览和入职的专精一致；混合不丢失；各招募途径规则明确 | 待处理 |
| F07 | 治疗药事件未应用 | App事件执行器 | 正负药水效果实际到账/扣除；远征携带与公会库存不互相覆盖 | **主线已部分修复（41b2a8b，先于本分支，合并自带）**：resolveEvent 已结算 potionHeal 并在结果面板显示影响明细；**残余=远征中触发事件的药水增减是否被"回城退回"覆盖**（run.potions 同步链），验收时按原标准复测 |
| F08 | 事件前置与后续幕混池 | data/guild-events、sim/guild-events、save | 后续幕只由合法前因进入；前置/地域策略先对齐；旧档兼容 | 待处理 |
| F09 | 挂机事件与重刷接续 | App回调、远征终局 | 无历史手动事件的新档可自动处理事件；整轮胜利后正确回城/重刷；失败停止 | 待处理 |
| F10 | 精英双倍掉落承诺 | run、App奖励、loot | 先明确翻倍的是概率/次数/数量；UI和实际收益一致，避免误改Boss奖励 | 待处理 |
| F11 | 第二版图杂兵掉落回退T1 | sim/loot、data/items | 按已确认装备纪元抽取；不因遗漏映射进入默认低阶池 | 待处理 |
| F12 | 招募冷却计量不一致 | App结算与酒馆 | 明确按战斗还是远征计；扣减和文案一致，缺员救济保留 | 待处理 |
| F13 | 重开公会确认与完整重置 | App restartGuild、标题页 | 内置浏览器兼容确认；一次确认后重置所有公会资产/建筑/日期/事件和委托，取消时不改存档；本轮仅接入委托清空 | 待处理 |

## 玩法设计待决项

| ID | 议题 | 决策前需要的信息 | 状态 |
|---|---|---|---|
| P01 | 核心体验与远征长度 | 用户选择主体验；记录整轮时长与有效决策 | 待对齐 |
| P02 | 永久死亡、光环、祝福与回滚 | 牺牲新人的收益/耗时；用户希望的严苛程度 | 待对齐 |
| P03 | 种族限制、混合转职与构筑 | 明确世界观限制还是概率偏好；已有阵容差异验证 | 待对齐 |
| P04 | 第二版图装备纪元 | 宪法T3/T4与后续提案T2高值区存在分歧 | 待对齐 |
| P05 | 固定地图还是固定节点池 | 希望玩家记住路线连边，还是节点风险与奖励 | 待对齐 |
| P06 | 高塔奖励和挂机定位 | 冲纪录/刷装/练级职责；自动停止和人工接管边界 | 待对齐 |
| P07 | 人物经营、事件因果与新手引导 | 首版体验指标；人物生活模拟的范围 | 待对齐 |
| P08 | 制作人挂起提案池（等 grilling，勿排期勿实现） | — | 待对齐 |

**P08 挂起提案池明细（2026-09-24 制作人侧登记，全部等制作人逐项 grilling 后才可动工）**：套装系统（件数效果）／声望系统／10 人本／公会保卫战／人物特性·非战斗向／亡灵登场叙事／版图三／经济方案池（特权训练/高塔保险/变卖通胀）／UI 换皮第三批（结算盖印文书/成员卡羊皮纸/战斗内 UI 材质，非优先）。上表 P01–P07 已于 2026-09-24 晚 grill 第一轮（结论见 DECISIONS.md），P08 未 grill，保持挂起。

## 当前建议顺序

D01 → F02/F03 → F01/F06 → F04/F05/F07/F09 → 奖励/事件规则对齐 → 第一版图体验验证 → 第二版图和长线经营。

不是排期承诺。执行时依据用户当前目标调整，不把全部任务绑成一个大改动。

## 每个实施切片的记录

在对应任务下按需补充：范围与不包含项、复现、实施要点、涉及文件、验证命令与结果、风险、提交/PR。计划是行动辅助，不要求低影响修改写冗长模板。
